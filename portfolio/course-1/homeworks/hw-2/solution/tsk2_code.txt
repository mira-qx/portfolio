int[] nums = { 9, 18, 27, 36, 45 };

Console.WriteLine("Прямой порядок:");

for (int i = 0; i < nums.Length; i++)
{
    Console.WriteLine($"{nums[i]}");
}


Console.WriteLine("Обратный порядок:");

for (int i = nums.Length - 1; i >= 0; i--)
{
    Console.WriteLine($"{nums[i]}");
}